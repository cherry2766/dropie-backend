# /var/lib/grafana/dashboards 폴더의 JSON을 부팅 때 자동 등록 (수동 import 불필요)
apiVersion: 1

providers:
  - name: k6
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards
